spring.application.name=LMS
server.port=${SERVER_PORT:9090}

# Active profile - can be overridden by SPRING_PROFILES_ACTIVE env var
spring.profiles.active=${SPRING_PROFILES_ACTIVE:default}

# Default profile settings - FORCE NEON DB CONNECTION
# Direct Neon DB configuration (working credentials confirmed)
spring.datasource.url=jdbc:postgresql://ep-cool-sound-adc8nufr-pooler.c-2.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_7wcg3zuZkWXx
# Default to PostgreSQL dialect; override with HIBERNATE_DIALECT if needed
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
# H2 console only relevant if using H2
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
spring.sql.init.mode=never
spring.flyway.enabled=false

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expirationMs=${JWT_EXPIRATION_MS:3600000}
# JVM Settings for production (can be overridden)
server.tomcat.threads.max=50
server.compression.enabled=true
